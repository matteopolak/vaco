[[source]]
id       = "alac-magic-cookie"
kind     = "spec"
title    = "Apple Lossless Format \"Magic Cookie\" Description (ALACMagicCookieDescription.txt)"
acquired = "2026-08-28"
where    = "https://raw.githubusercontent.com/macosforge/alac/master/ALACMagicCookieDescription.txt"

[[source]]
id       = "alac-readme"
kind     = "spec"
title    = "Apple Lossless (ALAC) reference package ReadMe.txt: supported bit depths, channel counts/orders, packet-size limits"
acquired = "2026-08-28"
where    = "https://raw.githubusercontent.com/macosforge/alac/master/ReadMe.txt"

# The above two are prose format-description documents Apple published
# alongside its reference decoder/encoder source under codec/ in the same
# repository. An earlier version of this file said vaco-codec-alac reads
# only these two files and never codec/'s C sources -- that was true only
# while this crate's predictor and entropy coder were a self-invented
# design that could not decode a real encoder's bitstream (see the
# now-superseded design note this replaced in frame_codec.rs and
# predictor.rs's own doc comments). It stopped being true once the owner
# confirmed (2026-08-28) that Apple's ALAC reference package
# (https://github.com/macosforge/alac) is licensed Apache License 2.0 --
# outside this project's D7/D15 clean-room rule, which is specifically
# about FFmpeg/libav-family GPL/LGPL code, not a blanket ban on every
# reference implementation. With that settled, this crate's rice.rs,
# predictor.rs and frame_codec.rs were rewritten as a from-scratch Rust
# translation of the algorithms in codec/ag_dec.c, codec/ag_enc.c,
# codec/aglib.h, codec/dp_dec.c, codec/matrix_dec.c,
# codec/ALACDecoder.cpp and codec/ALACAudioTypes.h -- see the
# `alac-agc-source` entry below and each module's own `Vaco-Spec-Ref`
# citations. The alac crate remains a dev-dependency oracle only, still
# probed as a black box for its observable input/output behaviour (see
# tests/oracle_alac_crate.rs) and never read as a coding reference --
# that boundary is unchanged; only the "never read codec/'s C sources"
# claim above it was wrong.

[[source]]
id       = "alac-agc-source"
kind     = "spec"
title    = "Apple Lossless (ALAC) reference codec source: adaptive Golomb-Rice coder (ag_dec.c, ag_enc.c, aglib.h), dynamic predictor (dp_dec.c), stereo mixing (matrix_dec.c), and top-level packet framing (ALACDecoder.cpp, ALACAudioTypes.h)"
acquired = "2026-08-28"
where    = "https://github.com/macosforge/alac (codec/ subdirectory), Apple Inc., Apache License 2.0 -- licence confirmed directly against https://raw.githubusercontent.com/macosforge/alac/master/LICENSE before any of this source was read"

[[source]]
id       = "alac-ffmpeg-blackbox-cookie"
kind     = "blackbox"
title    = "ALACSpecificConfig bytes as ffmpeg 8.1's ALAC encoder actually writes them, measured from a real output file"
acquired = "2026-08-28"
where    = "Local measurement, not a document: `ffmpeg -f lavfi -i \"sine=frequency=440:duration=0.2:sample_rate=44100\" -ac 1 -c:a alac mono.m4a`, then a throwaway Python MP4 box walk (no container-parsing crate) to locate moov/trak/mdia/minf/stbl/stsd's `alac` sample-entry box and hex-dump its trailing 24-byte ALACSpecificConfig. Done because alac-magic-cookie states maxRun \"should be set to 255\"; ffmpeg's real encoder writes 0 instead -- a spec-vs-reality gap in the same spirit as planning/AGENT-CONSTRAINTS.md's \"a name in the reference is not a specification\". The measured bytes are pinned as cookie.rs's real_ffmpeg_mono_cookie regression test and reused in tests/oracle_alac_crate.rs to cross-check this crate's cookie parser against the alac crate's own StreamInfo::from_cookie on identical input."

# No [[table]] rows: this crate has no hand-transcribed constant table of 32
# or more elements (no VLC table, no quantiser table -- the provenance
# README's threshold for a row here). rice.rs's and predictor.rs's
# adaptation constants (QBSHIFT, MB0/PB0/KB0, MMULSHIFT/MDENSHIFT/MOFF,
# MAX_PREFIX) are transcribed from alac-agc-source's aglib.h, not this
# crate's own invention -- see this file's history and each constant's own
# doc comment for the corresponding reference name.

[[source]]
id       = "alac-ffmpeg-caf-kuki-blackbox"
kind     = "blackbox"
title    = "ALAC magic-cookie wrapper shape as ffmpeg 9.0.1's CAF muxer actually writes it (the `kuki` chunk), measured from real output files at mono, stereo and 5.1 channel counts"
acquired = "2026-09-02"
where    = "Local measurement, not a document: `ffmpeg -f lavfi -i \"sine=frequency=440:duration=1\" -c:a alac -f caf out.caf` (plus a `pan` filter for the 5.1 case), then a throwaway Python CAF chunk walk (no container-parsing crate) to locate and hex-dump the `kuki` chunk. All three fixtures produced the identical 48-byte `frma`/`alac` \"Compatibility\" wrapper (`ALACMagicCookieDescription.txt`'s second on-disk shape: `[Format Atom][ALAC Specific Info header][24-byte config]`) with no trailing `ALACChannelLayoutInfo`, even at 5.1 -- the reference's own CAF encoder never emits that optional extension at any channel count tried. Used to resolve a real disagreement found while consolidating `vaco-codec-alac`'s and `vaco-parse-audio-misc`'s previously-independent `AlacSpecificConfig` parsers: one took the *last* 24 bytes of its input (silently wrong if a real cookie ever carries a trailing `ALACChannelLayoutInfo` after this wrapper), the other always read from the *front* (silently wrong for the crate's own `[size]['alac'][version+flags][config]` shape, which has a fixed-size prefix, not a wrapper with its own magic). The reconciled parser in `vaco-parse-audio-misc::alac` detects each wrapper by its own magic bytes rather than guessing from length, verified against this fixture plus a constructed (not reference-producible) case with a trailing `ALACChannelLayoutInfo` appended by hand."
