[[table]]
name   = "PROGRESSIVE_SCAN"
file   = "crates/codec/vaco-codec-prores/src/scan.rs"
source = "smpte-rdd36-2022"
clause = "SS7.2.2, Figure 4 (progressive block scan pattern)"
method = "transcribed"

[[table]]
name   = "INTERLACED_SCAN"
file   = "crates/codec/vaco-codec-prores/src/scan.rs"
source = "smpte-rdd36-2022"
clause = "SS7.2.2, Figure 5 (interlaced block scan pattern)"
method = "transcribed"

# dc_diff_codebook/run_codebook/level_codebook/decode_alpha_run/
# decode_alpha_difference (coeff.rs) were refactored from constant lookup
# arrays into functions (a match over the previous symbol selecting one of
# a small, fixed set of Codebook values) at some point after the rows below
# were written, so none of the five is a `static`/`const` array any more --
# `cargo xtask provenance-check` correctly flags all five as stale. Removed
# rather than left pointing at something that no longer exists; each
# function's own doc comment in `coeff.rs` still names its Table 9/10/11/
# 12/13/14 clause.
