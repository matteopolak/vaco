# Release artifacts (QA-10, #182): binaries, checksums, SBOM -- rebuilt
# fresh per release by scripts/package-release.sh, never committed.
/dist

/target
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
Cargo.lock.bak
*.profraw
/pgo-data
/bench-results
.DS_Store

# Stray media generated while probing the reference implementation.
/*.dv
/*.mp4
/*.mkv
/*.ts
/*.obu
/*.wav
