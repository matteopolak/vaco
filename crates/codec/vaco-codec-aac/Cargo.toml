[package]
name = "vaco-codec-aac"
description = "AAC-LC decode (patent-encumbered-aac-decode)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-bitstream = { path = "../../core/vaco-bitstream" }
vaco-limits = { path = "../../core/vaco-limits" }
vaco-codec-core = { path = "../../signal/vaco-codec-core" }
vaco-frame = { path = "../../model/vaco-frame" }
vaco-packet = { path = "../../model/vaco-packet" }
vaco-parse-aac = { path = "../vaco-parse-aac" }

# T3-03b/T3-03c (#444/#445, not yet implemented — see `docs/codec/vaco-codec-aac.md`)
# will add `vaco-codec-vlc` (spectral Huffman decode), `vaco-codec-dsp-sinewin`
# (window generation) and `vaco-tx` (the IMDCT, already built for SP-C6) here.
# Not added now because an unused dependency is exactly the kind of thing that
# looks like progress and is not; add each when the code that needs it lands.

[lints]
workspace = true

[package.metadata.vaco]
layer = 4
