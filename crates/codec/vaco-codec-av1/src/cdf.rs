//! Per-tile CDF context: §8.2.2's `init_symbol`'s "make a `Tile*Cdf` copy of
//! every default array" step, scoped to the syntax elements this crate's
//! intra-only decode path actually reads.
//!
//! Stub — filled in alongside the tile decode loop.
