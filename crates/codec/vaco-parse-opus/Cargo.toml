[package]
name = "vaco-parse-opus"
description = "Opus header parsing (no decode)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-bitstream = { path = "../../core/vaco-bitstream" }
vaco-codec-core = { path = "../../signal/vaco-codec-core" }
vaco-chlayout = { path = "../../model/vaco-chlayout" }
# `sample_fmt` is the decoder's output format, which the reference prints for
# every compressed audio stream. See the note beside `to_codec_parameters`.
vaco-sampfmt = { path = "../../model/vaco-sampfmt" }
vaco-limits = { path = "../../core/vaco-limits" }
vaco-packet = { path = "../../model/vaco-packet" }
arrayvec.workspace = true

[dev-dependencies]
proptest.workspace = true
divan.workspace = true

[[bench]]
name = "parse"
harness = false

[lints]
workspace = true

[package.metadata.vaco]
layer = 4
