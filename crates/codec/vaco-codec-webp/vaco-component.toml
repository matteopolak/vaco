# Registry fragment (plan 19 §3.4). Consumed by `cargo xtask gen-registry`.

[[component]]
kind      = "decoder"
name      = "webp"
long_name = "WebP: native VP8L lossless, `image-webp` fallback for VP8X-wrapped files"
feature   = "codec-webp"
ctor      = "vaco_codec_webp::WEBP_DECODER"
media     = "video"
codec     = "webp"

[[component]]
kind      = "encoder"
name      = "webp"
long_name = "WebP: native VP8L lossless by default, lossy via vaco-codec-vp8 (-lossless 0)"
feature   = "codec-webp"
ctor      = "vaco_codec_webp::WEBP_ENCODER"
media     = "video"
codec     = "webp"
