//! T3 measurement/visualisation video filters — `planning/16-filters.md`
//! §4.2's `vaco-filter-scope` row, GitHub issue #480 ("FT-4.12g").
//! Ten implemented: `histogram`, `waveform`, `datascope`, `thistogram`,
//! `graphmonitor`, `agraphmonitor`, `pixscope`, `drawgraph`, `adrawgraph`
//! (the last two via GitHub issue #473, FT-4.10 — see `vaco-filter-
//! draw-vf`'s own doc for that issue's full scoping), `vectorscope`.
//!
//! # Scoped the way #478 was scoped, before writing code
//!
//! `planning/ASSIGNMENTS.md`, every sibling `FT-4.12*`/`FT-4.11` GitHub
//! issue, and the generated registry were checked before this crate was
//! created, to confirm the row was genuinely unclaimed (posted as a comment
//! on #480 first — see that thread for the full elimination):
//! palette/stack/the overlay family belong to **#111** (a separate, still
//! open issue); the T1-tier `vaco-filter-scale` remainder (`scale2ref`,
//! `colorspace`, `colordetect`, `pixdesctest`, `zoompan`) is a real but
//! separately-unticketed gap, not claimed here; the twelve names in this
//! crate's row are the actual unclaimed T3 remainder.
//!
//! # What is verified versus structural versus not attempted
//!
//! | Filter | Status |
//! |---|---|
//! | [`histogram`] | Framecrc-level for the single-plane case: the bar-height formula (`ceil(count/max * level_height)`) and the `scale_height` gradient are both measured exactly against the reference, at two different count ratios to rule out a rounding coincidence. Multi-plane `stack` display is a documented, unverified extrapolation of the same rule. |
//! | [`waveform`] | Framecrc-level for `mode=column`, `mirror=true` (the default): the accumulation model (`intensity*255` added per hit, into a `255-v`-indexed row) is measured directly, including that it truly accumulates rather than saturating on the first hit. `mode=row` and `mirror=false` are not implemented. |
//! | [`datascope`] | **Structural, not framecrc-level, and permanently so.** The bitmap-font hypothesis (see below) held: the value-grid mechanism, the `passthrough`-format/independent-size link shape, the "canvas is always solid `0`, never a copy of the source" rule, and the source-sample-to-grid-cell mapping (`x`/`y` offsets, raster order) are all measured directly against the reference and implemented for `mode=mono`. But this crate draws with its own independently-sourced font (`crate::font8x8`, Unscii, not the reference's embedded table — a D7 requirement), so no frame containing text can ever match the reference byte-for-byte, no matter how exactly everything else is measured. `mode=color`/`color2`, `axis`, `opacity`, RGB pixel formats and multi-plane `components` are not implemented; see the module's own doc for exactly what was and was not probed. |
//! | [`thistogram`] | **Framecrc-level for `slide=replace` (the default) and `slide=frame`.** No text is drawn — every row/column is data — so unlike `datascope` this one has no permanent verification ceiling. The reference turned out to be *stateful*: a persistent `width x 256` canvas that gets exactly one new column per frame, confirmed with a 4-frame sequence. The per-column intensity formula (`round(count/max*255)`, checked against three ratios including an exact `0.5` tie, to rule out `histogram`'s own `ceil` rule as well as plain truncation) and both measured `slide` ring-buffer behaviours (`replace` overwrites only its own column; `frame` clears the whole canvas on wraparound) are exact. `slide=scroll`/`rscroll`/`picture` are rejected at creation with a clean error rather than guessed at. |
//! | [`graphmonitor`] | **Structural, not framecrc-level, and permanently so — the real consumer that proves `planning/INTERFACE-GAPS.md` gap 22 closed as a *solution*, not just a capability.** `graphmonitor`/`agraphmonitor` gained `vaco-filter-core::FilterContext::graph_nodes`/`graph_links` (gap 22); wiring them here, with a real `Graph`-based end-to-end test (`tests/graphmonitor.rs`, deliberately broken and restored to confirm it has teeth), is what settles that they serve these two filters, not merely that the accessors exist. Measured: `rgb24`-in-the-reference/`Gray8`-here output at exactly `size`; redrawn from scratch every frame; **rate-gated**, not one-in-one-out (a `10fps` source at `rate=2` for `2s` gave `5` frames, not `20`); one block per graph node (self included, plus the scheduler's own auto-inserted nodes) at three measured, non-uniform inter-line pitches (`10`/`12`/`15`px) chased to the pixel rather than approximated, since unlike `datascope`'s fixed grid this filter's line count varies with the graph. Not implemented: fields `NodeView`/`LinkView` genuinely cannot supply (`format`/`size`/`rate`/`timebase`, `pts`/`time` and their deltas — a real finding about gap 22's own deliberately narrow scope); fields available but not drawn by choice (the reference's paired `frame_count_in`/`out`/`delta` needs a push-side counter this crate does not keep, so this module shows every *other* `LinkStats` field instead); `mode=compact`/`nozero`/`noeof`/`nodisabled`; `opacity`; colour. See the module's own doc for the full accounting. |
//! | [`vectorscope`] | **Shipped, framecrc-level, for `mode=gray`/`tint` (default), `envelope=none` (default), `graticule=none` (default) — the previous pass's "unresolved nonlinearity" was a wrong measurement method, not a hard problem.** The blocker was building a controlled *hit count* by varying frame size, which conflates two different things: the reference's `256x256` histogram is rebuilt **from scratch every frame** (no persistence — confirmed by aiming one frame at cell A and the next four at cell B; A goes dark immediately), so any single-frame count is capped at that frame's own pixel count, and the earlier curve-fit was really fitting to whatever count a given frame size happened to produce, not to a clean independent variable. Holding a single frame's *pixel count at one target cell* fixed while everything else varies (paint exactly `N` pixels of a much larger frame with the test chroma, the rest off-cell) isolates count from frame size and gives an exact, unfitted rule: `per_hit = floor(255*intensity)` computed **once**, then `Y = clamp(count*per_hit, 0, 255)` — confirmed at 35 independent `(count, intensity)` pairs, including the two points (`count=50`/`100` at the default `intensity=0.004`) where a naive `round(255*count*intensity)` is off by one and this formula is not. The coordinate mapping (`col = value(component_x)`, `row = 255-value(component_y)`, inversion tied to the *axis*, not the component identity) was already right in the previous pass. The chroma marker is a second exact, binary rule found in the same probe: `127` if the cell was touched at all this frame, else the neutral `128` — not a gradient. Because the output canvas is a fixed `256x256 yuv444p`, same as the reference (no `size` option exists), this is the first filter in this crate shipped against the reference's *own* pixel format rather than this crate's usual `gbrp`/`gray8` substitute — nothing here forecloses framecrc identity the way this crate's font ceiling does elsewhere. |
//! | [`pixscope`] | **Structural, not framecrc-level, and permanently so — shipped after a prior finding was corrected in the open.** A previous pass reported the "zoom" window as an unmagnified location marker; that was measured against an all-black source *below* the reference's own undocumented `640x480` minimum (`"min supported resolution is 640x480"`, also newly found). Against a real source above that floor, the window **does magnify** (`7x7` source pixels to a fixed `294x294`px block grid, `42`px/pixel exactly). The five statistics are each pinned at four independent points (a flat field, a single-outlier probe, a symmetric ramp, and an edge-clamped ramp): `AVG`/`RMS` use the arithmetic mean and raw-value RMS respectively (not the median, not an AC/deviation-from-mean RMS), and `STD` is the **population** standard deviation (divide by `N`, confirmed against a visibly different number a sample `N-1` divisor would print). The sampled window's pixel bounds are `round(coord*dimension) - 1`, a measured constant `-1` bias independent of `w`/`h`, confirmed at three anchors and two window sizes; at a frame edge the window clamps (shifts to stay on-screen) rather than shrinking or wrapping. Implements `x`/`y`/`w`/`h`/`wx`/`wy` for `yuv444p`/`gray`-family and `gbrp`-family (`G,B,R` plane order) 8-bit sources; declines packed RGB, subsampled/alpha formats, and bit depths above 8. Not implemented: `o` (opacity — drawn fully opaque); colour-coding (every field drawn in one colour, since no two rows need one to tell them apart); the panel's exact reference pixel columns (a readable approximation, chased only as far as framecrc-exactness could ever pay for, which the font ceiling already forecloses). |
//! | `oscilloscope` | **Still not shipped, but `sc`'s "scope" geometry is now fully pinned — it is a single rotating dashed LINE, not a box or parallelogram, and the earlier "dashed parallelogram" read was the trace and the line conflated together.** Isolated with `c=0` (trace off) diffed against `sc=0`: every `sc=1` pixel at any `t` lies on exactly one straight line (checked by testing that every hit satisfies one `row=col+k` or `row=-col+k` equation, not a closed 4-sided shape). Measured exactly: **angle** `= (0.5-t) * pi` (vertical at `t=0`/`t=1`, horizontal at `t=0.5`, the two diagonals at `t=0.25`/`0.75`, confirmed against all five); **half-length** `= s * sqrt(w^2+h^2) / 2`, clipped at the frame edge when it would exceed the axis-aligned dimension (confirmed proportional to the frame *diagonal*, not `w`/`h`, at five `s` values, then confirmed the diagonal orientation is the one case that does NOT clip at `s=0.8`, exactly as that formula predicts); **centre** `= (round(x*w)-1, round(y*h)-1)`, the same `round(...)-1` bias `pixscope`'s `window_start` already uses. **The dash-phase finding the coordinator specifically asked for**: period is exactly 2 raster pixels (`255`, `0`, `255`, `0`, ...), and the phase is anchored to the line's own geometric endpoint, not to absolute frame-pixel parity — confirmed by shifting the line's start row by fractional amounts of `y` and finding the first pixel is `255` regardless of whether that row is even or odd. Further pinned down: the `255` end is always the endpoint reached by walking `+half_length` in the direction `(cos(angle), -sin(angle))` from centre (standard math-angle-to-pixel, y-flipped) — confirmed consistently across the vertical, horizontal and one diagonal case, so it is a property of the angle's sign, not of which coordinate happens to be smaller. **This pass's dispatch asked for the trace's sampling rule before its blend, and that rule is now confirmed structurally, though not yet a complete formula.** Using a source whose value varies by *row only* (constant across each row) and watching for a distinguishable spike against that flat background: at `t=0.5` (a horizontal scan) the trace is invisible at that row — because sampling *that row itself* gives the same constant value the background already has, so there's nothing to see; at `t=0.25`/`t=0` (tilted/vertical scans) a genuine bright spike appears, which a fixed-row sample could not produce. **The trace samples along the tilted `sc` scan line's own geometry, not a fixed row or column** — confirmed by this asymmetry, not assumed from the option names. What is still open: the exact column-to-line-position mapping, whether `tx`/`ty`/`tw`/`th` relocates the drawn box only or also redefines the sampling geometry independently of `x`/`y`/`s`/`t`, and the blend arithmetic — a two-point fit against flat-luma probes lands near `opacity≈0.8` toward a near-black background (`~15`), matching most measured points but not all exactly, which this pass is recording as an unresolved residual rather than rounding away (the same discipline that caught `drawgraph`'s fake margin). `st`'s statistics text and `g`'s grid remain unmeasured for exact placement. Shipping just the `sc` line while erroring on the default `c=7` would misrepresent "oscilloscope" more than it would help, so this stays characterised, not shipped — closer than before, with the sampling question (the one the coordinator flagged as determining everything else) now answered structurally. |
//! | `ciescope` | Not attempted, and explicitly **not a D7 case, checked again this pass rather than assumed**: every `system` value (`ntsc`/`ebu`/`smpte`/`240m`/`apple`/`widergb`/`cie1931`/`hdtv`/`uhdtv`/`dcip3`) names a published international standard's primaries, the CIE 1931 standard observer data is public, and none of it carries `#360`'s "recalled from the standard or from an open-source decoder, indistinguishably" ambiguity — this is ordinary citable science, the same footing BT.709/BT.2020 already stand on. **New this pass**: `-h`/`ffprobe` confirm the reference's own output is `512x512 rgba64le` by default (`s` ranges `256..8192`) — 16-bit RGBA, a pixel format and bit depth no other filter in this crate has needed, on top of the already-known large rendering surface (spectral-locus rasterisation, gamut-triangle drawing, colour fill across three coordinate systems `xyY`/UCS/`Luv`). Not attempted: the combination of a new output format plus that rendering surface is a bigger single task than this pass's remaining time could responsibly measure, not a data-sourcing question. |
//! | [`drawgraph`] | **Shipped for `mode=line`/`slide=frame` (both defaults) — corrected 2026-08-28 after a coordinator-requested re-probe found the original margin fit and the `slide=frame` scroll model were both wrong.** The value-to-pixel mapping is now an exact, margin-free formula (`row = clamp(floor((max-v)/(max-min)*(height-1)), 0, height-1)`), matched at 77 independently measured points (11 heights, 7 values each); the earlier "margined" formula was fitted to columns read under a wrong assumption about `slide=frame`'s own layout, and disappeared entirely once measured with a confound-free method (a constant value, `slide=picture`, reading column `0`). The apparent "out-of-range is a different, edge-clamping rule" finding also dissolved: it is the same formula's own `.clamp()` doing the work, not a second rule. `slide=frame` itself does **not** scroll (that is `slide=scroll`, unimplemented) — it fills columns left-to-right from `0` and wipes the whole canvas back to `bg` once full, confirmed with a monotonically increasing per-frame value and watching the column count reset. `fg1..4`'s colour is a hex literal with a real byte-order bug: written `0xAARRGGBB`, applied as opaque `(B, G, R)` — R and B swapped, alpha always ignored — while `bg`, a normal `<color>`, is unaffected; **this is reproduced deliberately, bug-for-bug, not corrected**, since a differential-tested reimplementation must match the reference's actual paint, not what the option name implies. Not implemented: `mode=bar`/`dot`; `slide=replace`/`scroll`/`rscroll`/`picture`; `fg1..4` as genuine value-dependent expressions (only the constant-hex case is implemented). Reads metadata via gap 11's `Frame::metadata_get`, proven through a real `Graph` end-to-end test, not just unit tests of the pixel-mapping formula. |
//!
//! # The bitmap-font hypothesis (resolved: held)
//!
//! `oscilloscope`/`datascope`/`pixscope` were previously reported as
//! blocked on the same prerequisite as `drawtext`: a working
//! `TextRenderer` (fontdb, shaping, glyph cache — GitHub `FT-3.5`/#462),
//! itself blocked on a `rustybuzz` provenance question under D10. The
//! hypothesis tested this pass was that these three do not need that
//! stack at all — that the reference draws them with a small, compiled-in,
//! fixed-width bitmap font instead. It held, checked two ways: `ffmpeg -h
//! filter=datascope`, `-h filter=pixscope` and `-h filter=oscilloscope`
//! (`ffmpeg 8.1`) expose no font/fontfile/fontsize option on any of the
//! three; and pixel-dumping both `datascope`'s and `pixscope`'s rendered
//! output (an all-black and an all-white synthetic source through each)
//! shows crisp, non-antialiased glyphs on an exact pixel grid pitch, with
//! `pixscope`'s statistics-overlay glyphs visually matching `datascope`'s
//! digit family. This is a materially smaller prerequisite than #462 in
//! full, and does not touch that issue's `rustybuzz` question at all — see
//! `crate::font8x8`'s doc for the font itself and why it is not, and
//! cannot be, the reference's own table. A comment to this effect belongs
//! on #462, since it means two separate packages (`drawtext`'s shaped-text
//! stack, and this family's fixed-width blit) were sharing one blocked
//! issue. `graphmonitor`/`agraphmonitor` share the same font and the same
//! permanent text ceiling, confirmed directly rather than assumed once
//! they were wired up. `pixscope`'s own panel glyphs were read directly
//! off a real render for this pass's shipped implementation, the same
//! family confirmed once more.
//!
//! See `docs/filter/vaco-filter-scope.md` for the full framecrc table and
//! every measurement's exact command line.

#![forbid(unsafe_code)]

mod common;
pub mod datascope;
mod font8x8;
pub mod oscilloscope;
pub mod drawgraph;
pub mod graphmonitor;
pub mod histogram;
pub mod pixscope;
pub mod registry;
pub mod thistogram;
pub mod vectorscope;
pub mod waveform;

pub use registry::ScopeRegistry;
