[package]
name = "vaco-filter-vdsp"
description = "Shared video kernels used by more than one filter crate: currently scene_sad (per-plane sum-of-absolute-differences), the kernel behind scene-change and freeze/duplicate detection. Plan 16 SS4.1."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-frame = { path = "../../model/vaco-frame" }

[dev-dependencies]
proptest.workspace = true
vaco-pixfmt = { path = "../../model/vaco-pixfmt" }

[lints]
workspace = true

[package.metadata.vaco]
layer = 5
