# Registry fragment (plan 19 §3.4). Consumed by `cargo xtask gen-registry`.
#
# Seven of plan 16 §4.2's `vaco-filter-overlay` row's eight filters
# (`feedback` blocked on interface gap 24 — see
# `docs/filter/vaco-filter-overlay.md`).

[[component]]
kind = "filter"
name = "blend"
long_name = "Blend two video frames into each other."
ctor = "vaco_filter_overlay::blend::DESC"
media = "video"

[[component]]
kind = "filter"
name = "multiply"
long_name = "Multiply first video stream with second video stream."
ctor = "vaco_filter_overlay::multiply::DESC"
media = "video"

[[component]]
kind = "filter"
name = "mix"
long_name = "Mix video inputs."
ctor = "vaco_filter_overlay::mix::DESC"
media = "video"

[[component]]
kind = "filter"
name = "xmedian"
long_name = "Pick median pixels from several video inputs."
ctor = "vaco_filter_overlay::xmedian::DESC"
media = "video"

[[component]]
kind = "filter"
name = "xfade"
long_name = "Cross fade one video with another video."
ctor = "vaco_filter_overlay::xfade::DESC"
media = "video"

[[component]]
kind = "filter"
name = "displace"
long_name = "Displace pixels."
ctor = "vaco_filter_overlay::displace::DESC"
media = "video"

[[component]]
kind = "filter"
name = "remap"
long_name = "Remap pixels."
ctor = "vaco_filter_overlay::remap::DESC"
media = "video"

[[component]]
kind = "filter_dispatch"
name = "vaco-filter-overlay"
ctor = "vaco_filter_overlay::OverlayRegistry"
