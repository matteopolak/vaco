[package]
name = "vaco-format-fixtures"
description = "Real, measured codec configuration-record bytes shared by container test suites, so a convention change updates one place instead of N hand-copies."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]

[lints]
workspace = true

[package.metadata.vaco]
layer = 4
