[package]
name = "vaco-format-ebml"
description = "RFC 8794 EBML: VINTs, the element header, and a schema-agnostic reader/writer shared by Matroska's demuxer and muxer."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-io = { path = "../../io/vaco-io" }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true

[package.metadata.vaco]
layer = 4
