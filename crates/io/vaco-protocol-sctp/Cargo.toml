[package]
name = "vaco-protocol-sctp"
description = "SCTP (RFC 4960): the common packet header (CRC32c), chunk framing (INIT/INIT-ACK/COOKIE-ECHO/COOKIE-ACK/DATA/SACK/HEARTBEAT/HEARTBEAT-ACK/ABORT/SHUTDOWN/SHUTDOWN-ACK/SHUTDOWN-COMPLETE/ERROR), and a sans-io four-way-handshake association state machine with basic reliable data transfer."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-hash = { path = "../../core/vaco-hash" }
vaco-limits = { path = "../../core/vaco-limits" }
vaco-protocol-core = { path = "../../io/vaco-protocol-core" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true

[package.metadata.vaco]
layer = 2
