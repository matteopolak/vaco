[package]
name = "vaco-protocol-srtp"
description = "SRTP (RFC 3711): key derivation, per-packet keystream/IV construction, and HMAC-SHA1 authentication for RTP/RTCP payload encryption. Built on vaco-crypto's AES-CTR; adds HMAC-SHA1 to vaco-crypto rather than duplicating hmac wiring."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-crypto = { path = "../../core/vaco-crypto" }
vaco-limits = { path = "../../core/vaco-limits" }
vaco-protocol-core = { path = "../../io/vaco-protocol-core" }
vaco-rtp = { path = "../../model/vaco-rtp" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true

[package.metadata.vaco]
layer = 2
