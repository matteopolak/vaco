[package]
name = "vaco-protocol-rtmp"
description = "RTMP: the chunk stream layer (handshake, chunking/dechunking, protocol control messages). AMF/NetConnection/NetStream and the rtmp: Protocol impl land in a later package."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
vaco-core = { path = "../../core/vaco-core" }
vaco-limits = { path = "../../core/vaco-limits" }
vaco-protocol-core = { path = "../../io/vaco-protocol-core" }
vaco-time = { path = "../../core/vaco-time" }

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true

[package.metadata.vaco]
layer = 2
